package com.huatuo.customer.service;

import java.util.List;

import com.huatuo.customer.domain.XtDrugstore;

/**
 * 药店服务
 * @author 河南华佗在线
 *
 */
public interface XtDrugstoreService {
	/**
	 * 根据经纬度算距离最近的n个药店
	 * @param longitude
	 * @param latitude
	 * @param pageSize
	 * @return
	 */
	List<XtDrugstore> getDrugstoreByCoordinate(Double longitude, Double latitude, Integer pageSize);
}
